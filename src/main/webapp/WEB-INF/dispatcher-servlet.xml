<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans 
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context 
           http://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/mvc 
           http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- 1. Component scan covers ALL controller packages -->
    <context:component-scan base-package="com.civicfix.controller, com.civicfix.config" />

    <!-- 5. Enable Spring MVC Annotations -->
    <mvc:annotation-driven />

    <!-- 6. Default Servlet Handler (CRITICAL for static files) -->
    <mvc:default-servlet-handler />

    <!-- 3. Static Resources Mapping -->
    <mvc:resources mapping="/resources/**" location="/resources/" />

    <!-- 2. View Resolver for JSPs -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/" />
        <property name="suffix" value=".jsp" />
    </bean>

    <!-- 4. Multipart resolver for file uploads -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="maxUploadSize" value="10485760" /> <!-- 10MB max -->
    </bean>

</beans>
